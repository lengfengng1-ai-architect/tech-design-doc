---
name: tech-design-doc
description: 技术方案设计文档的演进式协作流程：理念前置结构 + 讨论-落稿-审阅循环 + 多角度审阅菜单。用于设计/评审任何主题的技术方案文档（架构设计、模块设计、数据模型设计等）。用户在设计过程中可随时多次调用"审阅一下"触发多角度审阅。
---

# 技术方案设计文档协作（Tech Design Doc）

一种演进式设计协作法：文档不是一次写成，而是"讨论 → 落稿 → 审阅 → 修订"的循环产物。
用户在设计过程中可**任意时间、任意多次**说"审阅一下"——每次都换一个角度切入。

## 文档结构约定（铁律）

**前半理念、后半工程**——先回答"为什么这样做"，再回答"怎么做"：

```
第一部分：理念与方向
  - 为什么做（问题是什么，不做的代价）
  - 核心观念（一句话能说清的中心思想，最好有隐喻）
  - 设计哲学（几条不可妥协的原则）
  - 前沿参照（业界/学术界同类方案各借了什么，附出处）
第二部分：工程落地
  - 现状盘点与标准对齐（已有设施不重复建设）
  - 模块边界与接口（门面/归属/调用关系）
  - 数据模型（表结构 + DDL，精确到索引）
  - 写入路径 / 读取路径（分开写）
  - 治理（权限/审计/删除/可见性）
  - 分阶段落地（每期有验收标准）+ 范围声明与待决策项
附录：产出点清单、模拟案例（具体数据形态演示）、DDL
```

## 协作循环

1. **先聊后写**：新概念/新方向先口头讨论对齐，用户拍板再落文档；别替用户做价值判断，
   有分叉用选项摆出来让用户选。
2. **落稿即精准**：表名/字段/接口/枚举一次写准；写入文档的每句话都要能被实现。
3. **修订全量同步**：改一个概念，grep 全文同步所有提及处（术语、案例、DDL、阶段表）；
   修订后必做**残留检查**（搜旧表述，确认无残留）。
4. **案例驱动**：每个机制都要有具体数据形态的模拟实例（一个连贯剧本贯穿）——
   案例即文档的测试，改设计时案例同步改。
5. **提交节奏**：阶段性成果分批 commit；用户没说提交就不提交。

## 多角度审阅菜单（"审阅一下"时执行）

每次选**一个主攻角度**深挖，别泛泛而谈；每轮审完报告按"🔴真问题/🟠应修/🟡建议"分级，
用户拍板后落文档。轮换使用：

| 角度 | 查什么 |
|---|---|
| **一致性残留** | 多轮修订后的跨章节矛盾；旧术语/旧方案残留（grep 验证） |
| **正确性** | 数据模型（约束/索引/枚举死状态）、并发语义、边界情况 |
| **场景走查** | 拿 3-5 个关键用例端到端推演每条链路，看哪里卡 |
| **对抗性** | 恶意输入/注入/投毒；"用户这样说会怎样"的边缘场景 |
| **更优解** | 同一效果的更优雅机制（派生 vs 物化、合并载体、统一函数）——**不是砍功能** |
| **接口与规范** | 路由/命名/契约是否符合项目自身规范（先守自己的规矩） |
| **部署拓扑** | 多副本/重启/扩缩容下的行为；进程内状态的分布式后果 |
| **连锁反应** | 刚落地的改动引入了什么新风险（新规则的衍生债） |
| **主权与边界** | 数据归属、模块依赖方向、跨域调用是否越界 |
| **可测试性/可观测** | 每层怎么测、出问题怎么查、eval 覆盖什么 |

## 真问题模式库（历史实战提炼的检查清单）

审阅时逐条过：

- **字符串编码上下文**：维度压进字符串（如 `campaign:x.budget`）→ 独立维度（列/namespace 段）
- **状态机死状态**：某状态在某语义下永远不会出现 → 删掉或说明归属
- **派生 vs 物化**：能从查询派生的状态就不要物化（少一个后台任务一类故障）
- **持久化注入**：会被记住并反复注入上下文的内容 = 攻击面；已知键白名单 + 隔离标记
- **归属错误**："我朋友预算 5 万"被抓成用户的——陈述提取要第一人称标记
- **静默拒绝**：校验拒绝用户明确输入时，必须回以澄清而非丢弃（体验断裂）
- **删除级联**：派生物（综合页/摘要）含源数据内容，删源必须级联派生物
- **多副本失效**：进程内事件/缓存只到本副本——TTL 兜底或版本戳，写明接受的不一致窗口
- **串行化覆盖**：多条写路径时，互斥手段必须覆盖所有路径（队列管不住 web 进程，用 PG advisory lock）
- **概念错配**：A 域的治理哲学（如不可变发布）别硬套到本质不同的 B 域（如可塑的记忆）
- **新规则的语法连锁**：去掉某约束后，依赖它的语法/工具不可用（如无唯一索引则 ON CONFLICT 报错）
- **投毒与值域**：写入校验不止结构，还要值域（荒谬值写入侧即拒）
- **自举问题**："关闭 X 的开关"本身若走 X 管道会死锁——开关要在管道之前检查

## 用户的裁决习惯（配合方式）

- 用户质疑设计时：先查证（读代码/文档）再给裁定，不辩解不硬撑；
- 用户提出的方向性直觉（"感觉不对"）往往是对的——认真对待，往深挖；
- 用户的"简化"指**机制更优**，不是砍功能——区分"更优解"与"分期裁剪"，后者要先确认；
- 大改动分批落文档，落完报位置，等用户过目。
